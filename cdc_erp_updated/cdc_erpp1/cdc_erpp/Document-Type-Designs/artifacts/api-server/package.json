{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set -a; [ -f ./.env ] && . ./.env; set +a; export NODE_ENV=${NODE_ENV:-development}; npm run build && pnpm run start",
    "build": "node ./build.mjs",
    "start": "set -a; [ -f ./.env ] && . ./.env; set +a; node --enable-source-maps ./dist/index.mjs",
    "seed-auth": "set -a; [ -f ./.env ] && . ./.env; set +a; npm run build && node --enable-source-maps ./dist/seed-auth.mjs",
    "seed-demo": "set -a; [ -f ./.env ] && . ./.env; set +a; npm run build && node --enable-source-maps ./dist/seed-demo.mjs",
    "seed-admin": "set -a; [ -f ./.env ] && . ./.env; set +a; npm run build && node --enable-source-maps ./dist/seed-admin.mjs",
    "seed-permissions": "set -a; [ -f ./.env ] && . ./.env; set +a; npm run build && node --enable-source-maps ./dist/scripts/seed-permissions.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/express-session": "^1.18.2",
    "@types/multer": "^2.1.0",
    "@workspace/api-zod": "workspace:*",
    "@workspace/db": "workspace:*",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2",
    "csv-parse": "^6.2.1",
    "date-fns": "^4.1.0",
    "drizzle-orm": "catalog:",
    "express": "^5",
    "express-session": "^1.19.0",
    "multer": "^2.1.1",
    "pino": "^9",
    "pino-http": "^10"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "esbuild": "^0.27.3",
    "esbuild-plugin-pino": "^2.3.3",
    "pg": "^8.20.0",
    "pino-pretty": "^13",
    "thread-stream": "3.1.0"
  }
}
